en:
  site_settings:
    zero_click_sso_enabled: "Enable zero-click SSO for silent providers."
    zero_click_sso_enabled_for_noisy_providers: "Enable zero-click SSO for `noisy` providers as well. These non-silent OAuth2 providers could shown an authentication window. Warning! Enabling this may in fact require more than zero clicks. We tried."
